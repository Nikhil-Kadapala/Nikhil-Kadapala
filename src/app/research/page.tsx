import Link from "next/link"; import { research } from "@/lib/content"; export const metadata={title:"Research",description:"Work on claim extraction, agent behavior, and the systems around knowledge."}; export default function Research(){return <><section className="page-head wrap"><p className="eyebrow mono">Research</p><h1>Useful is a harder target than accurate.</h1><p>Work on claim extraction, agent behavior, and the systems around knowledge.</p></section><section className="section"><div className="wrap"><div className="list">{research.map(r=><Link className="list-item" href={`/research/${r.slug}`} key={r.slug}><span className="mono muted">{r.year}</span><div><h3>{r.title}</h3><p>{r.summary}</p><div className="mono muted" style={{marginTop:12}}>{r.tags.join(" · ")}</div></div><span className="link-arrow">Read</span></Link>)}</div></div></section></>}
