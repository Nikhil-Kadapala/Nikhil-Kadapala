import Link from "next/link"; import { projects } from "@/lib/content"; export const metadata={title:"GitHub"}; export default function GitHub(){return <><section className="page-head wrap"><p className="eyebrow mono">GitHub</p><h1>The code behind the questions.</h1><p>A small, curated set of repositories. For everything else, visit the profile.</p></section><section className="section"><div className="wrap grid-2"><div><h2>Open source is part of the argument.</h2><p className="muted">The best way to understand the research is often to inspect the system that made the claim.</p><Link className="btn" href="https://github.com/Nikhil-Kadapala">Open full profile ↗</Link></div><div className="list">{projects.filter(p=>p.repo).map(p=><Link className="list-item" href={p.repo} key={p.slug}><span className="mono muted">repo</span><div><h3>{p.name}</h3><p>{p.summary}</p></div><span className="link-arrow">View</span></Link>)}</div></div></section></>}
