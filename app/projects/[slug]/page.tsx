import { notFound } from "next/navigation"; import Link from "next/link"; import { projects } from "@/lib/content"; export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}; export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return notFound();return <article className="prose-page"><div className="wrap prose"><p className="eyebrow mono">{p.kind}</p><h1>{p.name}</h1><p className="lead">{p.summary}</p><div className="meta mono muted">{p.status} · {p.tags.join(" · ")}</div><p>{p.detail}</p><p><Link className="btn" href={p.href}>Open project ↗</Link>{p.repo&&<Link className="btn" href={p.repo}>GitHub ↗</Link>}</p><p><Link className="link-arrow" href="/projects">Back to projects</Link></p></div></article>}
