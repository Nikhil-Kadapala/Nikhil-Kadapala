---
title: Fern template adoption
status: baseline-resolved
updated: 2026-08-24
owner: Nikhil Kadapala
html: ./migration-plan.html
changelog: ./CHANGELOG.md
---

# Fern template adoption

Living plan. Update this file when a decision, wave, or metric changes. The HTML briefing is the narrative snapshot; this MDX file is the tracker.

**Process for every wave:** plan → review → implement → test → PR → review → merge.

**Context:** `/context-save` at each meaningful boundary. Start the next session with `/context-restore`. Do not continue a wave across a compaction cliff.

**Do not implement a wave in the same session that only planned it.** Fresh session after restore.

## Settled decisions

| ID | Call | Meaning |
|---|---|---|
| D1 | OS + token discipline | Steal Fern workflow and “tokens live once.” Keep DESIGN.md values (amber, 54px cap, dark). |
| D2 | Existing URLs | `/writing`, `/research`, `/projects`, `/about`, `/github`. Never `/post`. |
| D3 | Dark only | No `next-themes`. Skip theme-provider. |
| D4 | DESIGN.md wins values | Fern wins the no-escape-hatch rule. Reconcile `app/globals.css` in wave 3. |
| D5 | First-person research voice | Steal AGENTS.md structure. Rewrite every sentence. Em dashes stay if they earn their place. |
| D6 | bun + oxlint + Next 15 | No `src/` move. No Next 16 as a template side effect. Add Zod in wave 2. |
| D7 | One writing pipeline + `type` | See taxonomy below. Not three equal MDX trees. |
| D8 | Discard contradicting WIP | Restored app pages to `main`. Kept only infra that the waves will use. |

## D7 content taxonomy

Fern uses one MDX pipeline and a `type`-like split (blog vs case study) rather than a `/blog` URL implying one genre. We do the same under `/writing/[slug]`.

### Long-form (one pipeline)

All essays, logs, explainers, and course notes are writing.

| `type` | Folder | Use |
|---|---|---|
| `case-study` | `content/writing/case-studies/` | Finished project or research system. Problem → approach → result. Standalone engineering posts land here; there is no `blog` / `eng-blog` type. |
| `build-log` | `content/writing/build-logs/` | Numbered entries in a series (`series` + `part`). |
| `research` | `content/writing/research/` | Explainer or opinion tied to a paper. Not the publication record itself. |
| `teaching` | `content/writing/teaching/` | Course material while TAing. |

Shared Zod fields: `title`, `date` (publication day `YYYY-MM-DD`, not a timestamp), `description`, `type`, `tags`, `draft`. Optional: `series`, `part`, `paper` (DOI or URL), `course` (e.g. `CS 6xx · Fall 2026`). Git history is created/updated. Optional `updated` is a wave-2 call if a post is materially revised.

Index: `/writing` with filters by `type`. Series pages can group `build-log` parts.

Voice: one `voice-nikhil.md` plus short overlays per `type` (narrative for case studies, citation-heavy for research, example-heavy for teaching).

### Catalogs (not the writing pipeline)

| Surface | URL | Role |
|---|---|---|
| Projects | `/projects`, `/projects/[slug]` | Shipped work cards. Links, status, repo. A case-study post may point here. |
| Research | `/research`, `/research/[slug]` | Publication record: year, venue, PDF, arXiv. A `type: research` post may point here. |

Do not flatten papers and repos into `/writing`. Do not create `/research/[slug]` MDX essays that duplicate `type: research` posts.

### Seed content to recategorize in wave 2

These files exist as drafts. They are not wired to routes after D8.

- `content/writing/the-score-is-not-the-work.mdx` → `type: research` (or opinion)
- `content/research/claim-extraction-checkthat-2025.mdx` → publication catalog, plus optional writing explainer
- `content/projects/*.mdx` → project catalog records, not case-study posts until we write real case studies

## Waves (one PR each)

| Wave | PR title (working) | Status | Import | Edit |
|---|---|---|---|---|
| 0 | Baseline: artifacts, CI, agent skills, shadcn/cn, discard 3-tree MDX | merged | — | — |
| 1 | Agent OS | **this change** | `AGENTS.md` shape, `writing` skill | Voice, paths, proof points. Skills live in `.agents/skills`. |
| 2 | MDX pipeline | not started | Zod, `draft`, registry, remark-gfm, `generateMetadata` | One loader. `type` enum. Recategorize seed MDX. Replace every wave-1 placeholder in the table below with concrete data. |
| 3 | Tokens + `cn()` | not started | `extendTailwindMerge` once size tokens exist | DESIGN.md values. No Fern green / 64px / 86rem. |
| 4 | Article chrome | not started | `.article-prose` idea | Map onto `.prose`. Dark, amber links, Commit Mono code. |
| 5 | Optional skills | not started | file-bug → visual QA; case-study → `write-project` | Skip campaigns, SEO factory, employee profiles. |

## Wave 2 — replace placeholders with concrete data

Wave 1 skills still talk about a future loader. When wave 2 ships, rewrite these so they describe the live system. Do not leave “until wave 2” in the writing skill.

| Location | Placeholder now | Replace with |
|---|---|---|
| `.agents/skills/writing/SKILL.md` Step 3 | “enforced by Zod in wave 2” | Actual schema module path (likely `lib/schemas.ts` or wherever it lands) |
| `.agents/skills/writing/SKILL.md` Step 3 | “seed inventory may still use `excerpt`” | Delete after seed files use `description` |
| `.agents/skills/writing/SKILL.md` Step 4 | “legacy seed files at root” | Delete after `content/writing/*.mdx` is recategorized into type folders |
| `.agents/skills/writing/SKILL.md` Step 5 | “MDX components and a full registry arrive in wave 2” | The real component names, or “standard Markdown only” if none shipped |
| `.agents/skills/writing/SKILL.md` internal links | “once wave 2 wires routes” | Live `/writing/<slug>` routes |
| `.agents/skills/writing/SKILL.md` Step 7–8 + checklist | Pre-Wave-2 inventory guard | Delete. Handback is a live URL. |
| `.agents/skills/writing/voice-nikhil.md` teaching | Example `CS 6xx · Fall 2026` | Real course code if known; otherwise keep as a format example |
| `lib/content.ts` + `app/writing/page.tsx` + RSS | Hardcoded `excerpt` array | Loader + `description` |
| `content/writing/the-score-is-not-the-work.mdx` | Root seed, `excerpt` frontmatter | Move to `content/writing/research/`, `description`, `type: research` |
| This file | “published through this pipeline once wave 2 exists” | Series slug `borrow-the-os-not-the-look`; rewrite this row when the live URL exists |

Optional `updated` field: decide in wave 2 whether a materially revised post gets `updated: YYYY-MM-DD` alongside `date`. Default is no — git history is enough unless the index needs to show “updated”.

## PR checklist (every wave)

- [ ] Plan written or updated in this file
- [ ] Plan reviewed (human)
- [ ] Implement only that wave
- [ ] `bun run typecheck` && `bun run lint` && `bun run build`
- [ ] Preview URL checked
- [ ] `/context-save` after merge
- [ ] Changelog entry (see CHANGELOG.md required fields)
- [ ] Fresh session via `/context-restore` before the next wave

## Explicit non-goals

- Fern visual identity
- `/post/[slug]`
- `campaigns/` and launch-campaign
- SEO blog factory
- Employee profiles
- Vale/CI prose lint (write 3–4 voice rules in AGENTS.md instead)
- Light theme
- `src/` directory move
- Next 16 as a template side effect
- Figma MCP as sole source of truth (code in the running app wins; paste screenshots when MCP drops assets)

## How we will work

- Outcome prompts, not step-by-step component prescriptions (Fern’s own lesson).
- New page or post = pull request + Vercel preview, even solo.
- `/components` and `/scrapyard` are optional later, not wave 1.
- The “how I adopted Fern” series is a `build-log`. Part 1 draft: `content/writing/build-logs/borrow-the-os-not-the-look.mdx`. Citeable claims: `./build-log-sources.md`. Live `/writing/[slug]` waits on wave 2.
